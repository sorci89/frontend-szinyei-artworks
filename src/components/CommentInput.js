import React from 'react';

function CommentInput({
  stars,
  comment,
  tag,
  loggedIn,
  onClick,
  isChoosen,
  setIsChoosen,
}) {
  return <div>CommentInput</div>;
}

export default CommentInput;
